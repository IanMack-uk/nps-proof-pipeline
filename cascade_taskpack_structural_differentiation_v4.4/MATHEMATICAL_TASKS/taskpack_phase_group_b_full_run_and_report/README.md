# Phase Group B — Full Maths Run Task Pack

Point Cascade to **TASK.md**.

This task pack runs:
0A → A → A′ → 0B → B → B′

…and writes a consolidated run report into the run directory under `cert_artifacts/`.
