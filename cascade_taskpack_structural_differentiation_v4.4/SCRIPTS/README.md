# Scripts

These are small helpers. Cascade may extend them as needed.

- `dag_extract.py` — placeholder wrapper for repo DAG generation (if your repo already has one, point to it)
- `check_import_policy.py` — placeholder; implement repo-wide phase import isolation checks
