# Cascade Task Pack — Task 10.4 Certification Dependency Integrity Gate

This pack adds a governance check that validates **CAS chain dependency order** and **cross-artefact consistency**.

## How to use with Cascade
1. Copy this folder into your repository.
2. Point Cascade to `TASK.md`.
3. Cascade implements the module + tests + report.
4. Verify tests pass in both default and CI-mode.
