# Phase C — Task 1 Report — Operator Layer

Run ID: RUN_ID_TODO  
Date (UTC):  
Generated by:  

Artifacts written (run root):
- OPERATOR_LAYER.json:
- PhaseC_TASK1_OPERATOR_LAYER_REPORT.md: (this file)

---

## 1) Inputs (authoritative)

Run root: `cert_artifacts/RUN_ID_TODO/`

Required:
- `CAS-A.json`
- `CAS-B.json`
- `PhaseC_ENTRY_REPORT.md`

Optional:
- `CAS-0C.json` (tool whitelist + dependency context)

Governance gates enabled:
- NPS_IMPORT_FIREWALL=1
- NPS_CAS_GATE=1
- NPS_CERT_DEP_GATE=1
- NPS_RUN_DIR_INTEGRITY=1

---

## 2) Objective convention and equilibrium regime (from CAS chain)

Certified objective convention (CAS-A):
- value:
- source field path:

Equilibrium objective + regime (CAS-B):
- equilibrium_objective:
- equilibrium_regime:
- source field path:

Consistency check:
- PASS / FAIL
- notes:

---

## 3) Operator definition

Equilibrium operator selected for Phase C:

\[
F(w,\theta) := \nabla_w \Phi_{\text{cert}}(w,\theta)
\]

Notes:
- Clarify whether \(\theta\) is present yet (if not, use \(F(w) := \nabla_w \Phi_{\text{cert}}(w)\) for Task 1).
- Record whether Phase C uses \(H := \nabla^2_w \Phi_{\text{cert}}\) or the coupling matrix \(C := -H\).

---

## 4) Structural-coordinate basis (Phase A)

Basis / ordering rule used:
- basis_id:
- definition:
- deterministic? yes/no

If an explicit permutation exists:
- permutation_P length:
- properties checked:
  - is a permutation: yes/no
  - maps to valid indices: yes/no

If no explicit permutation exists:
- document the ordering rule used in Phase C (must be deterministic).

---

## 5) Hessian object at equilibrium (shape + symmetry + convention)

Equilibrium point \(w^*\) reference:
- source field path:
- dim_w:

Hessian convention:
- H := ∇²_w Φ_cert(w*, θ)  (or w* only)
- C := -H  (if used)

Checks performed:
- symmetry check: PASS / FAIL
- dimension check: PASS / FAIL
- convention check (maximize vs −Φ, etc.): PASS / FAIL

---

## 6) Verification checklist (Task 1)

Fill this table using `phase_C_task1_verification_rules.md`.

| check_id | description | result | evidence | notes |
|---|---|---|---|---|
| | | PASS/FAIL | upstream / computed | |
| | | PASS/FAIL | upstream / computed | |

---

## 7) Decision

Task 1 status:
- DONE / BLOCKED

If BLOCKED:
- list blockers
- required follow-up taskpacks

