# Phase C — Task 5 Verification Rules — Locality and Sparsity Support Bounds

This file defines the **authoritative check list** for Task 5.

All check results must appear in the run-root report and in the corresponding JSON artefact(s).

## Tolerances

- Default numeric tolerance for symmetry / zero-tests: `1e-10` (override explicitly if needed)
- Default “structural zero” tolerance for sparsity tests: `1e-12` (override explicitly if needed)

## Required checks

### CHK.C5.SPARSITY.METRICS_RECORDED
- Description:
- PASS condition:
- FAIL condition:
- Evidence source: upstream / computed
### CHK.C5.LOCALITY.RADIUS_TESTED
- Description:
- PASS condition:
- FAIL condition:
- Evidence source: upstream / computed
### CHK.C5.LOCALITY.SUPPORT_BOUND_HOLDS
- Description:
- PASS condition:
- FAIL condition:
- Evidence source: upstream / computed

