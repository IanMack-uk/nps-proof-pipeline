# Phase C — Task 9 Verification Rules — M-Matrix Criteria and Inverse Positivity

This file defines the **authoritative check list** for Task 9.

All check results must appear in the run-root report and in the corresponding JSON artefact(s).

## Tolerances

- Default numeric tolerance for symmetry / zero-tests: `1e-10` (override explicitly if needed)
- Default “structural zero” tolerance for sparsity tests: `1e-12` (override explicitly if needed)

## Required checks

### CHK.C9.MMATRIX.PRECONDITIONS_PASS
- Description:
- PASS condition:
- FAIL condition:
- Evidence source: upstream / computed
### CHK.C9.MMATRIX.NONSINGULAR_CONCLUDED
- Description:
- PASS condition:
- FAIL condition:
- Evidence source: upstream / computed
### CHK.C9.INVERSE.NONNEGATIVE_CONCLUDED
- Description:
- PASS condition:
- FAIL condition:
- Evidence source: upstream / computed

