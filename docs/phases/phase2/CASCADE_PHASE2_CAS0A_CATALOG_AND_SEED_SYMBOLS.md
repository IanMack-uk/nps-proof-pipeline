# CASCADE PHASE 2 --- BUILD CAS-0A PROPERTY CATALOG + SEED SYMBOLS

Project: `nps-proof-pipeline`

Phase 1 (property-level registry) is complete. This Phase 2 task builds
the first **auditable CAS-0A property catalog** and seeds the **Phase A
SymbolDictionary** with a minimal, strictly sourced set of core objects.

This phase still avoids "new maths": it only encodes what is already
stated in **CAS-0A-v4** and (only where needed for Phase A objects)
**Paper 0 (Foundations)**.

------------------------------------------------------------------------

## 🎯 Objective

1.  Create a **CAS-0A property catalog** as code: a function that
    registers certified properties with `SourceRef` locators.
2.  Create a **minimal Phase A seed symbol set** that uses only those
    registered properties.
3.  Add **audit exports**:
    -   print / return a dependency table for CAS-0A properties
    -   (optional) write a Markdown report under `docs/specs/`

------------------------------------------------------------------------

## 0️⃣ Files to Edit / Add

### Edit

-   `src/nps/phases/phase_0a/build_cas0a.py`
-   `src/nps/phases/phase_a/build_casa.py`
-   `src/nps/certification/citations.py` (if needed for consistent
    SourceRef locators)
-   `tests/` (add 1--2 tests)

### Add

-   `docs/specs/cas0a_property_catalog.md` (generated by code OR
    hand-written from same sources)

------------------------------------------------------------------------

## 1️⃣ Define Standard Property Keys

In `src/nps/phases/phase_0a/build_cas0a.py`, define constants for the
most important CAS-0A certified properties.

Use the Phase-1 `PropertyKey(subject, property)` type.

Minimum set (extend later as needed):

### Ω (admissible domain)

-   `("Omega", "nonempty")`
-   `("Omega", "compact")`

### Φ (relational potential)

-   `("Phi", "continuous")`
-   `("Phi", "C3_extension")` (or the exact regularity required by
    CAS-0A text)

### Locality

-   `("Locality", "defined_neighborhood_N(e)")`
-   `("Locality", "uniform_radius_r")`

### Gradient / marginal structural field F

-   `("F", "defined_as_grad_w_Phi")`

### Equilibrium correspondence

-   `("W_star", "defined_as_argmax_over_Omega")`

Each property registration MUST include a `SourceRef` that points to the
specific CAS-0A locator (e.g., "Assumption A1", "Assumption A3",
"Definition 7.1", etc.).

------------------------------------------------------------------------

## 2️⃣ Implement `build_cas0a_registry(registry)`

In `src/nps/phases/phase_0a/build_cas0a.py`, implement:

``` python
def build_cas0a_registry(registry: DependencyRegistry) -> None:
    """Register CAS-0A certified properties with exact SourceRef locators."""
```

Rules: - Must call
`registry.register(PropertyKey(...), source=SourceRef(...))` for each
property. - Must NOT register any property without a SourceRef. - Must
NOT import Paper 1 here.

------------------------------------------------------------------------

## 3️⃣ Seed Phase A SymbolDictionary

In `src/nps/phases/phase_a/build_casa.py`, implement:

``` python
def build_phase_a_symbols(registry: DependencyRegistry) -> SymbolDictionary:
    """Create minimal Phase A symbols that only require CAS-0A properties."""
```

Seed symbols (minimum): - `Omega` as a `MathematicalObject`
(domain/codomain can be symbolic TypeSpec labels) - `Phi` as a
`MathematicalObject` - `F` as a `MathematicalObject` - `W_star` as a
`MathematicalObject` (equilibrium correspondence)

For each seeded symbol: - Provide explicit `domain`, `codomain`,
`regularity`, `locality`, `source` - Provide `requires={...}`
referencing the property keys registered in Phase 0A

This ensures SymbolDictionary enforcement is exercised in a realistic
way.

------------------------------------------------------------------------

## 4️⃣ Add at least 2 Tests

Create `tests/test_cas0a_property_catalog.py` with:

### Test A --- build registers all required keys

-   Create an empty registry
-   call `build_cas0a_registry(registry)`
-   assert that the minimum property keys exist

### Test B --- phase A seed symbols validate

-   Create registry and build CAS-0A properties
-   call `build_phase_a_symbols(registry)`
-   assert no exception and that expected symbols exist

------------------------------------------------------------------------

## 5️⃣ Add Audit Export (Recommended)

Add to `build_cas0a.py`:

``` python
def cas0a_dependency_rows(registry: DependencyRegistry) -> list[tuple[PropertyKey, SourceRef]]:
    """Return a stable, sortable list for audit reporting."""
```

Optionally generate `docs/specs/cas0a_property_catalog.md` containing: -
each PropertyKey - its SourceRef document + locator

------------------------------------------------------------------------

## 6️⃣ Verification

All must pass:

    pytest
    mypy src
    ruff check .

------------------------------------------------------------------------

## ✅ Completion Condition

Phase 2 is complete when:

-   CAS-0A properties are registered individually with SourceRef
    locators
-   Phase A seed symbols require only those properties
-   At least two tests cover catalog registration + Phase A symbol build
-   Auditable dependency rows are exportable
-   All checks pass
